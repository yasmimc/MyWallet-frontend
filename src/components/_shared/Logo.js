import styled from "styled-components";

export default function Logo() {
	return <Container>My Wallet</Container>;
}

const Container = styled.h1`
	font-family: Saira Stencil One;
	font-size: 32px;
	color: #ffffff;
	margin: 24px;
`;
